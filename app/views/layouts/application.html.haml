!!!
%html
  %head
    %title Evaluator
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
  #navigation
    %ul
      - if signed_in?
        %li= link_to current_user.github_handle, person_path(current_user)
        %li= link_to "Sign out", signout_path

  = yield

