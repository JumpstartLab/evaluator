%ul
  - evaluation_responses.group_by(&:evaluation_id).each do |evaluation_id, responses|
    %li
      = Evaluation.find(evaluation_id).title
      %ul
        - responses.each do |resp|
          %li= link_to resp.person, admin_evaluation_response_path(resp)
